{
  "name": "tablecloth-mcp",
  "version": "0.2.0",
  "description": "MCP server that finds Korean e-Gov/finance sites and opens them in a clean, disposable Windows Sandbox with the required security software. Discovery + safe-launch only (no RPA). Node/TS implementation (counterpart of the .NET dnx build). Dual-licensed: AGPL-3.0-or-later, or a commercial license.",
  "type": "module",
  "bin": {
    "tablecloth-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "mcpName": "io.github.yourtablecloth/tablecloth-mcp",
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourtablecloth/TableClothMcp.git"
  },
  "homepage": "https://yourtablecloth.app/TableClothMcp/",
  "license": "AGPL-3.0-or-later",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "tablecloth",
    "korea",
    "windows-sandbox",
    "egov"
  ],
  "scripts": {
    "gen": "node scripts/gen-shared.mjs",
    "build": "npm run gen && esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --banner:js=\"#!/usr/bin/env node\"",
    "typecheck": "tsc --noEmit",
    "conformance": "node test/conformance.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "fast-xml-parser": "^4.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
