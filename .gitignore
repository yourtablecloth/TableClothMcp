# .NET build output (root-anchored so it doesn't match npm/**/bin)
/bin/
/obj/
*.user
.vs/
# npm / release assembly
node_modules/
dist/
artifacts/
# mcpb / registry assembly
mcpb-build/
mcpbtest/
*.mcpb
server.tmp
# dev
smoke.out
